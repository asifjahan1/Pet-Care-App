import 'package:flutter/material.dart';

class Treatement extends StatelessWidget {
  const Treatement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
